const ENDPOINTS = {
    BASE: import.meta.env.BACKEND_URL,
    VERSION: import.meta.env.BACKEND_VERSION,
    EVENTS: "events",
    AUTH: 'token',
    USER: 'users',
    PLATES: 'plates',
}


export {
    ENDPOINTS
}